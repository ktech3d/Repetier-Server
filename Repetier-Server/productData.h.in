// This file contains definition, that come from the cmake configuration. That way
// CMake values can be used inside the server itself.

#define PROJECT_MAJOR_VERSION @Server_VERSION_MAJOR@
#define PROJECT_MINOR_VERSION @Server_VERSION_MINOR@
#define PROJECT_NAME "@PROJECT_NAME@"
#define PROJECT_NAME_VERSION "@PROJECT_NAME@ @Server_VERSION_MAJOR@.@Server_VERSION_MINOR@"
